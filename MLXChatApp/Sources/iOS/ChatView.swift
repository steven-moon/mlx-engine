import SwiftUI

struct ChatView: View {
    var body: some View {
        VStack {
            Spacer()
            Text("Chat UI Coming Soon")
                .font(.title2)
                .foregroundStyle(.secondary)
            Spacer()
        }
        .navigationTitle("Chat")
    }
} 